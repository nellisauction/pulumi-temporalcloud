// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The provider type for the temporalcloud package. By default, resources use package-wide configuration
 * settings, however an explicit `Provider` instance may be created and passed during resource
 * construction to achieve fine-grained programmatic control over provider settings. See the
 * [documentation](https://www.pulumi.com/docs/reference/programming-model/#providers) for more information.
 */
export class Provider extends pulumi.ProviderResource {
    /** @internal */
    public static readonly __pulumiType = 'temporalcloud';

    /**
     * Returns true if the given object is an instance of Provider.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Provider {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === "pulumi:providers:" + Provider.__pulumiType;
    }

    /**
     * The ID of the account to operate on. Prevents accidental mutation of accounts other than that provided.
     */
    declare public readonly allowedAccountId: pulumi.Output<string | undefined>;
    /**
     * The API key for Temporal Cloud. See [this documentation](https://docs.temporal.io/cloud/api-keys) for information on how to obtain an API key.
     */
    declare public readonly apiKey: pulumi.Output<string | undefined>;
    /**
     * The endpoint for the Temporal Cloud API. Defaults to `saas-api.tmprl.cloud:443`.
     */
    declare public readonly endpoint: pulumi.Output<string | undefined>;

    /**
     * Create a Provider resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ProviderArgs, opts?: pulumi.ResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        {
            resourceInputs["allowInsecure"] = pulumi.output((args?.allowInsecure) ?? utilities.getEnvBoolean("TEMPORAL_CLOUD_ALLOW_INSECURE")).apply(JSON.stringify);
            resourceInputs["allowedAccountId"] = (args?.allowedAccountId) ?? utilities.getEnv("TEMPORAL_CLOUD_ALLOWED_ACCOUNT_ID");
            resourceInputs["apiKey"] = (args?.apiKey ? pulumi.secret(args.apiKey) : undefined) ?? utilities.getEnv("TEMPORAL_CLOUD_API_KEY");
            resourceInputs["endpoint"] = (args?.endpoint) ?? utilities.getEnv("TEMPORAL_CLOUD_ENDPOINT");
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["apiKey"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(Provider.__pulumiType, name, resourceInputs, opts);
    }

    /**
     * This function returns a Terraform config object with terraform-namecased keys,to be used with the Terraform Module Provider.
     */
    terraformConfig(): pulumi.Output<Provider.TerraformConfigResult> {
        return pulumi.runtime.call("pulumi:providers:temporalcloud/terraformConfig", {
            "__self__": this,
        }, this);
    }
}

/**
 * The set of arguments for constructing a Provider resource.
 */
export interface ProviderArgs {
    /**
     * If set to True, it allows for an insecure connection to the Temporal Cloud API. This should never be set to 'true' in production and defaults to false.
     */
    allowInsecure?: pulumi.Input<boolean | undefined>;
    /**
     * The ID of the account to operate on. Prevents accidental mutation of accounts other than that provided.
     */
    allowedAccountId?: pulumi.Input<string | undefined>;
    /**
     * The API key for Temporal Cloud. See [this documentation](https://docs.temporal.io/cloud/api-keys) for information on how to obtain an API key.
     */
    apiKey?: pulumi.Input<string | undefined>;
    /**
     * The endpoint for the Temporal Cloud API. Defaults to `saas-api.tmprl.cloud:443`.
     */
    endpoint?: pulumi.Input<string | undefined>;
}

export namespace Provider {
    /**
     * The results of the Provider.terraformConfig method.
     */
    export interface TerraformConfigResult {
        readonly result: {[key: string]: any};
    }

}
